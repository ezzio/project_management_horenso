import axios from "axios";
import { API } from "./configApi";

const settingApi = {
  renameProjectAsync: (params) => {},
  deleteProjectAsync: (params) => {},
  transferOwnerShipAsync: (params) => {},
};

export default settingApi;
